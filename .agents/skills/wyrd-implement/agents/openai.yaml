interface:
  display_name: "Wyrd Implement"
  short_description: "Persist through one bounded Wyrd task"
  default_prompt: "Use $wyrd-implement to complete this approved Wyrd task, autonomously repairing reversible implementation and verification failures."

policy:
  allow_implicit_invocation: true
