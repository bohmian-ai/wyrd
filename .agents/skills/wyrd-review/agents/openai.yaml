interface:
  display_name: "Wyrd Review"
  short_description: "Review Wyrd conformance or supply static review policy"
  default_prompt: "Use $wyrd-review to adversarially falsify this Wyrd implementation's correctness, proof, architecture fit, simplicity, and consumer closure. In the $wyrd-implement-plan-v2 binding, perform the primary review, coordinate its required Terra-medium code-quality specialist under review-core, and return one consolidated static result without writing another artifact, running verification, or routing work."

policy:
  allow_implicit_invocation: true
