interface:
  display_name: "Wyrd Implement"
  short_description: "Complete one bounded Wyrd task or prove it blocked"
  default_prompt: "Use $wyrd-implement to execute this one approved Wyrd task through verified completion or a genuine escalation blocker."

policy:
  allow_implicit_invocation: true
