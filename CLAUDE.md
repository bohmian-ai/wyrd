@AGENTS.md

!IMPORTANT: Never execute shell commands for cargo (lints, test, fmt, check, build, run, etc.). This will cause unnecessary delays and contention
on the built artifacts. Run all commands in session.