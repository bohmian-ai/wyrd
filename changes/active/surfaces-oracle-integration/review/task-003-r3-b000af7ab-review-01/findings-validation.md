# Wave 2 findings validation — TASK-003-R3

## Subject and independent check

Base `861f8d86cc3f9d7e70fb59489e80f8be62afddbf`; candidate `b000af7ab704f077a8a4ba3e29c2b968d47d344d` (tree `f087f7d395cd53386e4e2c6f011b31d8604fd791`). Source was last changed at `f8e887b3038651d2ba82091d642915d6b856d4d6` (tree `eb98062dc854277a248f37d86afe55beb9c3db26`). I read all four Wave 1 reports, the R3 packet/evidence, approved revision-9 REQ-064/INV-025/AC-022, original TASK-003, `AGENTS.md` §16, `architecture/agent-rules.md`, `rust-core.md` documentation rule, the changed R3 source, its direct callers and tests, and `mise.toml`'s gate dependency list. `.codegraph/` is absent. No implementation or broad test was changed or run in this validation.

## Validation of every Wave 1 proposal

| Wave 1 proposal | Decision | Independent reason |
|---|---|---|
| `TASK-R3-1` final-tree aggregate proof | **CONFIRMED** | `mise.toml:1361-1415` defines `gate` as a pure 49-dependency aggregate, so same-tree children give substantial functional coverage, but the R3 packet records killed `gate` attempts and only a grouped assertion for 48 other children. Approved REQ-064/AC-022 and original TASK-003 explicitly require the broad gate to pass locally; R3 explicitly authorizes child substitution for `verify:bifrost`, not for `gate`. A prior `gate` pass at `117f668f6` predates R3 source. No test failure is inferred from the host-memory kill. |
| `STD-R3-1` incomplete new-item rustdoc | **CONFIRMED** | `forwarding.rs:118,1043` introduces undocumented tuple fields on `Abandon` and `DropProbe`. Its new `silent_selected_delivery_times_out_once_and_is_cancelled` test at `:1052` has `expect` and assertions but no `# Panics`. `AGENTS.md` §16 and `rust-core.md` make these test-only/private items mandatory documentation, not optional style. The journey test already documents errors and panics. |
| `STD-R3-2` qualified type positions | **CONFIRMED** | `architecture/agent-rules.md:9-10` requires top-module imports and bare names in fields, signatures, return types and `where` clauses. New `SilentForwardPeer` fields, `wait_abandoned` return, local `Abandon` field, test `DropProbe` field, and `Bifrost::silent_forward_peer_for_test` return use qualified paths. `route_remote_once` is materially changed in R3 and its bound still uses a qualified `VisibilityMode`. The rule does not prohibit the body-local constructor or ordering paths. |
| Remote-forwarding domain report: no finding | **VALIDATED EMPTY** | `ReadyOracleForwarder::forward` has one production call to `route_remote_once`; the other three calls are inline tests. The full `route_remote_once` body uses the ingress `Instant` for both connect and selected delivery, maps timeout to `QueryTimeout`, and has no post-delivery retry. The selected delivery is `forward_remote` through initial peer response, while the returned stream remains under its prior contract. |
| Peer-security domain report: no finding | **VALIDATED EMPTY** | `SilentForwardPeer::hold_if_armed` has one caller in the test-support-gated private handler. `ReadyOracleForwarder::silent_peer_for_test` and `Bifrost::silent_forward_peer_for_test` lead only to the gated journey. The ordinary peer authentication and signed-ticket acceptance path remain intact. Parking before ticket verification is test-only and proves the intended connected-but-silent transport case; it is not a production-bypass finding. |

## Deduplicated retained findings

### `FIND-TASK-003-R3-1` — final-tree broad gate result absent

- **Source:** `TASK-R3-1` (**CONFIRMED**). **Classification:** MISSING. **Obligation:** approved REQ-064/INV-025/AC-022, original TASK-003 verification, and R3's final-tree matrix require a passing local broad gate without waiver or omitted tests.
- **Location and reachable consequence:** `TASK-003-R3-close-r2-review-findings.md:163-194` records `mise run gate` killed by memory pressure and claims its 49 dependencies passed separately, but does not record a passing gate result or an individually auditable command/result row for every dependency. This is an acceptance-evidence gap on the final source tree, not proof of incorrect runtime behavior.
- **Minimum correction:** No implementation change, new runner, or duplicated tests. Run the existing `gate` aggregate to exit 0 on the final source tree, with no concurrent compiler jobs; installed mise supports `mise run -j 1 gate` to serialize its task dependencies. Record the command, exit status, source commit/tree, and nonzero test selections. If even a serialized aggregate cannot run, an explicit approved change to the broad-gate acceptance obligation is necessary before treating child execution as equivalent; the R3 packet's `verify:bifrost` exception does not grant it.
- **Focused closure proof:** a completed `mise run -j 1 gate` result on the immutable final source tree, with all dependencies unfiltered and passing. Its own dependency closure supplies the broad proof; do not add a checker for the checker.

### `FIND-TASK-003-R3-2` — new test items lack required rustdoc

- **Source:** `STD-R3-1` (**CONFIRMED**). **Classification:** VIOLATION. **Obligation:** `AGENTS.md` §16 and `rust-core.md` Documentation require meaningful rustdoc for new private/test fields and functions, including `# Panics` when possible.
- **Location and reachable consequence:** `crates/wyrd/wyrd-server/src/oracle/forwarding.rs:118,1043,1052`. The RAII `Abandon` field is used by `hold_if_armed` to count a dropped peer handler; the `DropProbe` field is used only by the new paused-time timeout test. Their tuple fields have no docs, and that assertion-bearing test has no `# Panics`. The code works but misses a hard repository completion rule.
- **Minimum correction:** Document each tuple field's owned/reference state and its role in drop observation; add a concise `# Panics` section to the new test naming its cancellation, one-delivery, and one-connect assertions. Keep the two narrow existing probes and all cancellation behavior; no new abstraction or test harness is warranted.
- **Focused closure proof:** source audit of those exact items, then `mise run fmt:check` and `mise run lints` on the corrected tree. The existing exact paused-time test already exercises the probes.

### `FIND-TASK-003-R3-3` — new field/signature types bypass required import style

- **Source:** `STD-R3-2` (**CONFIRMED**). **Classification:** VIOLATION. **Obligation:** `architecture/agent-rules.md:9-10` requires top-of-module `use` declarations and bare type names in fields, signatures, return types, bounds, and `where` clauses.
- **Location and reachable consequence:** `crates/wyrd/wyrd-server/src/oracle/forwarding.rs:71,73,104,118,598,1043` and `crates/wyrd/wyrd-server/src/state.rs:1735`. These are reached by the gated peer handler and role-separated journey, the paused-time test, or the production `forward` call to the materially changed `route_remote_once`. Qualified type paths at those positions violate the repository's dependency-manifest convention; body-local qualified constructors are not part of this finding.
- **Minimum correction:** Add only the needed module-top imports, feature-gated for test-only types, and use bare names at the identified positions: `AtomicBool`, watch sender/error types, `VisibilityMode`, and `SilentForwardPeer`. The `route_remote_once` body and all security, timeout, feature, and public API behavior remain unchanged. No new type alias or module is needed.
- **Focused closure proof:** inspect the identified type positions, then run `mise run fmt:check` and `mise run lints` on the corrected tree; rerun the exact forwarding unit test and owning server journey as the narrow behavior checks before the mandatory final-tree gate.

## Prior R2 findings and limits

`FIND-TASK-003-R2-3` is closed in source: the selected remote delivery is bounded by the captured deadline and has typed timeout/cancellation/one-attempt coverage. `FIND-TASK-003-R2-2` is closed: the previously missing edge-layer associated-type and `# Errors` rustdoc is present. The six specifically omitted owner lanes in `FIND-TASK-003-R2-1` have passing nonzero same-source-tree results; its broader final-tree gate requirement remains open as `FIND-TASK-003-R3-1`. `verify:bifrost` has the exact-child alternative expressly allowed by R3; local real-cloud tasks are sufficient proof under approved revision 9. The first-wave reviewers did not rerun all credentialed or heavy lanes, and no raw logs independently verify the packet's grouped gate-child assertion. These limits do not create an additional finding or justify weakening a gate.
