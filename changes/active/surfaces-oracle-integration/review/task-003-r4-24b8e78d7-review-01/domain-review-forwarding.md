# Oracle forwarding and edge-timeout domain review

**Subject:** cumulative `861f8d86cc3f9d7e70fb59489e80f8be62afddbf..24b8e78d7420321eb8fe936b3045fcbd3bc1d26b` (candidate tree `6a3ec967ab094a42256b44ce908abb080360e466`). The tested source is `d6de890d83f269eab834fa323f3ebe31f1adc573` (tree `d8a727b5846f24bac7b55bbc786bcb391c55cb03`); the later commit changes the R4 evidence packet only. No `.codegraph/` index exists. Overall domain result: **PASS**.

| Boundary and authority | Source and verification inspected | Result |
|---|---|---|
| Protected HTTP admission and Oracle handoff: original TASK-003, R2/R3/R4 packets; `AGENTS.md` §§9, 11, 16; approved spec revision 9; `architecture/bifrost-design.md` query deadline and terminal contract | `http/router.rs` protected stack; `http/middleware/edge_timeout.rs:119-145`; `query/routes.rs:229-263`; `query/service.rs:259-280`. The generic edge limit remains active through body extraction, authentication and capability admission. Only `POST /v1/query` receives the handoff signal; after admission, dispatch is awaited under Oracle ownership. Existing local preparation and slow-body/non-query controls are in `wyrd-testing/tests/bifrost/server/query.rs:1208-1310`. | PASS |
| One captured deadline, selected delivery, typed error, cancellation and no successor: spec REQ-015 and query deadline contract; Bifrost design distributed execution and terminal contract; R3 `FIND-TASK-003-R2-3` | `oracle/forwarding.rs:228-300` captures monotonic and signed wall deadlines once; `:592-618` bounds connected-peer delivery with the remaining time and maps expiry to `BifrostError::QueryTimeout`; `:388-446` shows that delivery includes credential acquisition and first gRPC response. The `FnOnce` delivery occurs after candidate selection, and no successor path follows its error. Exact unit test at `:1075-1127` asserts typed timeout, drop of pending delivery and one attempt. | PASS |
| Peer trust and test-only hook: `AGENTS.md` server/tenancy rules; Bifrost design authenticated peer boundary; R3/R4 constraints | `oracle/peer_service.rs:470-499` retains `peer_context`, required envelope, open Gate and role check before the `#[cfg(feature = "test-support")]` pause; ordinary `forwarder.accept(ticket)` still verifies the signed claims. `oracle/forwarding.rs:69-137`, `oracle/mod.rs`, and `state.rs:1734-1741` feature-gate the switch. The role-separated journey at `wyrd-testing/tests/bifrost/server/query.rs:1315-1417` asserts the typed public error, one received envelope, abandoned peer handler, and a successful subsequent forwarded query. R4 imports/doc changes do not alter these branches. | PASS |
| R4 docs/import correction and production isolation: `AGENTS.md` §16; `architecture/agent-rules.md` top-level imports/bare type names | The R4 source diff is only `oracle/forwarding.rs` and `state.rs`: two tuple-field docs and a `# Panics` section, top-level imports, bare names in the identified fields/signatures, and `test-support` gating for test-only imports. No runtime statement, feature definition, auth condition or timeout expression changes. Evidence records all-feature lints, default-feature server check, exact forwarding unit test, and server journey pass on the tested source tree. | PASS |

**Verification limits:** I did not rerun the large suites or live-cloud tasks in this review. The R4 packet records a completed passing gate and the other same-tree lanes; gate-proof equivalence is outside this forwarding-domain judgment. The role-separated journey proves that the timed-out HTTP request finishes and a following request succeeds, not a direct per-node permit count; source ownership shows the request future returns on timeout and the protected stack's concurrency guard then drops. The pre-selection candidate-connect error mapping predates this cumulative change and is outside the selected-delivery correction.

**Material findings:** none. No changed forwarding or edge path violates the approved Oracle deadline, one-attempt, auth/tenant or cancellation obligations.
