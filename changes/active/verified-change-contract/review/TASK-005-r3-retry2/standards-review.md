# TASK-005 round 3 repository standards review

## Immutable subject and authorities

- Root: `/home/thorrester/Documents/GitHub/wyrd-vcc-t005`.
- Base: `f8811ac5035c3aa165d34c38992f9889b3c9081f`.
- Candidate: `0ef7208565c69d96c44dfe5316394764678bd4e6` (the last remediation commit). Later HEAD `d638cc90` adds only the blocked review record. This report judges the cumulative base-to-candidate diff. `.codegraph/` is absent.
- Authority: `AGENTS.md`, `architecture/agent-rules.md`, `architecture/wyrd-design.md`, `architecture/wyrd-doctrine.mdx`, `architecture/bifrost-design.md`, `architecture/wyrd-security-posture.md`, the approved specification revision 36, original TASK-005, and the applicable `architecture/references/` routes named below. Prior review reports were used only to identify closure obligations, not as authority for source compliance.

## Authority coverage and rule results

| Changed surface | Applicable authority | Rule result and source evidence |
|---|---|---|
| Active spec/task, architecture drift and security text, prior review/evidence artifacts | `AGENTS.md` §§1–2, 12, 14–16; `references/languages/spec-driven-development.md`, `implementation-execution.md`; `wyrd-design.md`, `wyrd-doctrine.mdx`, `wyrd-security-posture.md` | **PASS**. The task frontmatter now says `status: review`, `spec_revision: 36`; its outcome, owner, approach, Scenario 3 and acceptance text identify a scoped SYSTEM token and fixed SQL via the query service. The older path occurs only in historical evidence. This closes `REPO-R2-001`. Revision 36 is approved. |
| `wyrd-spec` Card validation/status, generated JSON schemas and TS status projection | `AGENTS.md` §§2–4, 8–9, 16; `wyrd-design.md`; `references/doctrine/positioning-and-vocabulary.md`, `architecture/patterns.md`, `languages/rust-core.md`, `languages/errors.md` | **PASS**. `wyrd-spec/src/card/{drift,verifier}.rs` keeps validation and status types pure, with no IO, async or PyO3. The generated schema files and `sdks/wyrd-sdk-ts/wyrd/src/index.ts` expose the matching typed baseline state. The packet records `codegen:check` green. |
| SYSTEM token, permission and verifier, query service, scheduled consumer, Drift SQL | `AGENTS.md` §§2–6, 9–10, 16; `agent-rules.md` tenant and audit rules; `wyrd-security-posture.md`, `bifrost-design.md`; `references/architecture/patterns.md`, `domain/olap-serving.md`, `languages/agent-harness.md` | **PASS**. `wyrd-auth/src/issuance.rs` mints a single table-UID permission; `wyrd-auth-verify/src/lib.rs` requires one closed SYSTEM purpose; `verification/drift.rs` uses `ScheduledQueryCaller` and the query service, preserving Gate/Oracle read authorization and auditing. No local Oracle query-plan path was added. |
| Vala fit/score and baseline fitter/verification runtime | `AGENTS.md` §§3–6, 10–11, 16; `agent-rules.md` struct, async, rustdoc and testing rules; `references/languages/rust-core.md`, `domain/drift-monitoring.md`, `domain/evaluation.md`, `domain/arrow-analytical-interop.md`, `domain/analytical-operations-reliability.md` | **PASS**. `vala-drift/src/baseline/mod.rs` adds a synchronous cancellable fit entry point; `psi/mod.rs` and `spc/mod.rs` poll within fit steps; `verification/fitter.rs` passes the token to `spawn_blocking` and awaits completion before lease settlement. State and workflow remain on `BaselineFitter`, `DriftEngine`, and scorer structs. New functions and tests carry intent and error/panic rustdoc. The 256 MiB decode limit and shared permits remain. |
| Baseline SQL, Forge namespace and migrations | `AGENTS.md` §§2–3, 9, 11, 16; `agent-rules.md` `TenantConn`, `OperatorPool`, RLS and field mapping; `bifrost-design.md`; `references/domain/iceberg.md`, `olap-serving.md`, `analytical-operations-reliability.md` | **PASS**. `wyrd-sql/src/queries/drift_baselines.rs` uses tenant connections for row operations and the operator pool for due-tenant discovery; the baseline migration uses tenant keys and RLS. Forge Rust and both migration CHECK lists accept `vala.verification`, with a synchronization test. No domain-library `PgPool` propagation or callee `TenantConn` commit was added. |
| CLI fixture changes; Rust, Python and TS SDK journeys; PyO3/N-API test server; mise runner; lockfile | `AGENTS.md` §§3, 7–8, 11–12, 16; `agent-rules.md` journey/runtime ownership and gate integrity; `references/languages/pyo3-boundaries.md`, `python-api-and-stubs.md`, `typescript-guide.md`, `testing-workflows.md`, `implementation-execution.md` | **PASS**. Each language's new journey uses its own runtime and a real server; `mise.toml` and `scripts/run-bifrost-journeys.sh` include the Drift journey, including TS. The N-API test-server option composes the existing verification runtime. Python declarations match the harness option and recorded `codegen:check`; no new Python-only durable logic appears. The Oracle capacity assertion retains the equality and retries within its existing bounded cleanup window. |

## Applicable cross-cutting checks

| Rule | Result | Evidence |
|---|---|---|
| Repository ownership and language-agnostic durable server contracts (`AGENTS.md` §§2–3, 7–9) | **PASS** | Scoring is in Vala Rust, orchestration in `wyrd-server`, SQL in `wyrd-sql`, first-class SDK additions are journeys and typed projection. |
| Struct-centered Rust, narrow async, imports, documented changed items (`AGENTS.md` §§5–6, 16; `agent-rules.md`) | **PASS** | `BaselineFitter`, `DriftEngine`, `ObservationWindow`, `SharedTrigger` own cohesive workflows; cancellable computation remains synchronous; sampled new/changed items carry rustdoc and fallible `# Errors` where applicable. |
| Tenant, authorization and audit boundaries (`AGENTS.md` §§2, 9; `agent-rules.md`) | **PASS** | SYSTEM claim shape is closed; query reads pass through existing Gate/Oracle authorization and audit; SQL queue uses tenant connections and operator discovery. Domain reviewers own deeper behavioral validation. |
| Test tiers, exact commands, generated artifacts and gate integrity (`AGENTS.md` §§8, 11–12; `agent-rules.md`) | **PASS** | The r2 evidence records the two new exact focused commands and post-change `fmt`, `lints`, Vala, Wyrd and all nine Bifrost lanes green; original task evidence records SQL, SDK, storage, codegen and boundary checks. No failing check was disabled. The added `#[ignore]` marks real Postgres journeys selected by their explicit `--run-ignored=all` lane. `git diff --check f8811ac5 0ef72085` is clean. |
| Prohibited unrelated or speculative changes (`AGENTS.md` §§1–2, 12, 15) | **PASS** | Skill-file edits were reverted. The Oracle capacity test fix addresses an observed failure while preserving its assertion. `Oracle::query_plan` predates the task and is unchanged. |

## Findings and limits

No material repository-rule finding is proposed. I inspected the complete changed-file inventory and cumulative diff, with focused review of every owner layer and the r2 remediation delta. The recorded lane outcomes are implementation evidence, not tests rerun by this reviewer. Independent task and domain reviewers determine whether the behavior itself meets every acceptance criterion.

**Overall: PASS.**
