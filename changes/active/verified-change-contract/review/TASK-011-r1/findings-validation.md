# TASK-011 Wave 2 finding validation

**Immutable subject:** `338f33235f81c30dfe3a570dc26934fe7bb77048..6e3bac0370a31b19d767ac4d20830d430c3f2ff5`. Approved spec: revision 37. I inspected the complete diff, the approved spec and task, repository authorities, and all four Wave 1 reports. `.codegraph/` is absent. This is a static validation; I did not alter or rerun the candidate.

**Overall: SPEC_REVISION_REQUIRED.** The direct Arrow scorer cannot distinguish a selected observation whose configured feature is null from an unrelated all-null row. REQ-156 requires opposite outcomes for those two rows, requires direct/server agreement, and TASK-011 excludes a new observation format. The approved contract must choose a way to represent selected-row membership before that finding can receive an implementation remedy. The remaining findings have bounded corrections under revision 37.

## Caller and reachability checks

- `TargetColumn::carried` and `target_complete` have exactly the direct `score_psi` and `score_spc` callers; `score_drift` dispatches to them. I read those full bodies and the `FittedPsiFeature::count`/`numeric_values` counting paths. The direct API accepts a wide `RecordBatch` with nullable configured columns and no per-row series-presence field. Its existing unrelated-row tests intentionally use all-null rows. Server `ObservationWindow::incomplete` instead selects by a present `series` row, even when its value is null. The discrepancy is reachable through the public direct functions; it is not a server dispatch defect.
- `SpcScorer::finish` is called by direct `score_spc`, server `DriftEngine::try_verify`, and direct/server tests. I read `SpcScorer::push`, `finish`, `score_spc`, server `fold_spc`, and `DriftReport::aggregate_verdict`. A complete out-of-limit feature plus a partial feature yields `Drift` because common aggregation prioritizes drift. The server can reach that shape through historical/external duplicate series rows, and the public scorer can reach it directly. The correction belongs in the SPC scorer, not the common verdict aggregator or Custom.
- `Reader::fold` is called by `Reader::complete` and the Custom, PSI, and SPC branches of `try_verify`; I read the complete bodies. Each call mints a fresh token and starts a separate authenticated query. Oracle pins one cut per query (`architecture/bifrost-design.md`), so rev37's new completeness check and later aggregates can observe different cuts. A normal ingest into a manual window after the check is reachable. This is a new consequence of the rev37 completeness obligation, even though separate queries predate this task. The correction belongs in the PSI/SPC server read orchestration; Custom and the authorization/audit path should stay intact.
- The changed `SpcProfile` derives the served schema; `mise.toml` places `pg_openapi_contract` in `test:principals:integration`, outside the recorded Bifrost server lane. The task's Vala focused-command template is not an executable command for any named test. Task status is still `ready` although the reference workflow defines `review` for submitted implementation. These are reachable evidence/workflow gaps, not statistical code defects.
- AC-034 explicitly says real Rust, Python, and TypeScript SDK-to-server journeys must prove failed **scheduled** dispatch and visible **legacy-version** refusal. The Rust journey does both using `VerificationFixture::make_binding_due` and `retire_fitted_format`. Python and TypeScript only prove manual binding dispatch and registration rejection of removed `weco_rule`; neither is a scheduled occurrence or a stored legacy fit. The language test servers exist and can be extended at their test-only boundary; no production API is needed.

## Validated ledger

### FIND-TASK-011-1 — REVISED; INCORRECT; spec decision required

**Sources:** TASK-REV-011-1, STAT-1. **Obligation:** REQ-156 and TASK-011 input completeness, including selected-null refusal, unrelated-row exclusion, and direct/server agreement. **Location:** `crates/vala/vala-drift/src/feature.rs`, `TargetColumn::carried` and `target_complete`; direct callers in `psi/mod.rs::score_psi` and `spc/mod.rs::score_spc`; contrast `wyrd-server/src/verification/drift.rs::ObservationWindow::incomplete`. **Evidence:** `carried` treats `None` as absent, so a selected all-null row vanishes from direct counts or subgroup order while server selects a null-valued series row and refuses scoring. The same wide Arrow row also represents the existing intentionally unrelated all-null row. **Consequence:** direct PSI/SPC can score a window the server calls inconclusive, and SPC can shift groups. **Decision needed:** approve a direct-input row-membership representation or explicitly choose which all-null rows belong to the target; revise the no-new-observation-format constraint as needed. A nullness-only guard cannot meet both requirements. **Closure proof after decision:** one selected null-only row amid enough valid data returns a whole unscored PSI/SPC report; one genuinely unrelated all-null row remains excluded; equivalent server observations agree. No public API shape is prescribed before approval.

### FIND-TASK-011-2 — CONFIRMED; INCORRECT

**Source:** STAT-2. **Obligation:** REQ-154 and REQ-156 require a partial or insufficient SPC target to make the *run* inconclusive with no scored details or feature rows. **Location:** `crates/vala/vala-drift/src/spc/mod.rs::SpcScorer::finish` and `report.rs::aggregate_verdict`. **Evidence:** `finish` emits an inconclusive feature alongside a scored drifting feature; shared aggregation returns `Drift`. **Consequence:** server result publication can mark an incomplete window failed and dispatch an Operator. **Smallest correction:** have the existing SPC scorer return `DriftReport::unscored(Spc)` when any feature has zero complete subgroups or a trailing partial subgroup; leave common aggregation and Custom unchanged. **Closure proof:** a two-feature scorer input with one signaled complete feature and one partial/empty feature produces an inconclusive report with no features; server aggregate fixture with duplicate series presence produces an inconclusive result and no dispatch. Retain complete-input chart fixtures.

### FIND-TASK-011-3 — CONFIRMED; INCORRECT

**Source:** DOMAIN-PERSIST-1. **Obligation:** REQ-156 requires completeness and scoring of the same selected target window; REQ-153/154 require one consistent PSI population and SPC subgroup sequence. **Location:** `crates/wyrd/wyrd-server/src/verification/drift.rs::DriftEngine::try_verify`, `Reader::complete`, `Reader::fold`. **Evidence:** completeness and each feature aggregate run as distinct query-service requests, each with its own pinned cut. New observations can enter a future-containing manual window between requests. **Consequence:** a run can publish scores/feature rows for a target that became incomplete between reads, or combine feature populations from different cuts. **Smallest correction:** use the existing authenticated, audited query-service stream for one fixed PSI/SPC statement that computes completeness and all required aggregates from one Oracle cut; keep exact filters, bounded result handling, and current publication/dispatch behavior. Do not introduce a direct Oracle or cross-query snapshot facility. **Closure proof:** interleave an incomplete observation at the former read-phase seam for PSI and SPC and show no mixed-snapshot scored result/feature rows; run Drift journeys plus query/audit checks.

### FIND-TASK-011-4 — CONFIRMED; VIOLATION

**Source:** STANDARDS-011-1. **Obligation:** `AGENTS.md` §11 requires the served OpenAPI gate for a public schema change. **Location:** changed `wyrd-spec/src/card/drift.rs::SpcProfile`; TASK-011 verification record. **Evidence:** `test:principals:integration` (containing `pg_openapi_contract`) was not run; the recorded Bifrost server lane excludes it. **Consequence:** the composed served `/openapi.json` contract remains unverified. **Smallest correction/proof:** run `mise run test:principals:integration` at the corrected candidate and record its result, fixing any actual failure without weakening the gate.

### FIND-TASK-011-5 — CONFIRMED; VIOLATION

**Source:** STANDARDS-011-2. **Obligation:** `AGENTS.md` §11 and `architecture/agent-rules.md` require an exact focused command and result for every named Rust test in a task artifact. **Location:** TASK-011 “Focused, one exact command per named test.” **Evidence:** `test(=<path>)` is a placeholder for 23 named Vala tests. **Consequence:** per-test proof is not reproducible from the committed packet. **Smallest correction/proof:** record each real fully qualified test expression and exact `mise exec -- cargo nextest run --locked -p vala-drift --lib -E 'test(=...)'` command with its result; run any command lacking actual prior execution evidence.

### FIND-TASK-011-6 — CONFIRMED; VIOLATION

**Source:** STANDARDS-011-3. **Obligation:** `architecture/references/languages/spec-driven-development.md` task lifecycle. **Location:** TASK-011 front matter `status: ready`. **Evidence:** implementation evidence and immutable review submission exist, so `review` is the defined state. **Consequence:** the task packet misstates its lifecycle to reviewers and planners. **Smallest correction/proof:** set status to `review` in the committed evidence update; leave the approved spec untouched.

### FIND-TASK-011-7 — REVISED; MISSING

**Source:** independent validation of the Wave 1 acceptance matrix and its stated SDK limits. **Obligation:** AC-034 and TASK-011 acceptance explicitly require real Rust, Python, and TypeScript SDK journeys to prove a failed scheduled run's Operator dispatch and visible refusal of a stored legacy version. **Location:** `sdks/wyrd-sdk-python/tests/integration/test_drift_journey.py`, `sdks/wyrd-sdk-ts/wyrd/tests/integration/drift-verification.test.ts`; compare Rust `sdks/wyrd-sdk-rust/tests/drift_verification.rs`. **Evidence:** Python/TypeScript call a manual binding run and reject an authored `weco_rule` during registration; they never trigger a due occurrence or retire a fitted baseline. **Consequence:** two required client journeys do not cover those cross-boundary outcomes. **Smallest correction:** reuse the existing `WyrdTestServer` and Rust `VerificationFixture` test-only mechanisms for making a binding due and retiring a fit, projecting only the narrow fixture operations needed into Python/TypeScript test servers; assert scheduled failure/dispatch and later legacy refusal through each real SDK. Do not add product routes or duplicate scheduler/baseline logic in SDKs. **Closure proof:** exact focused Python/TypeScript journey commands exercise each operation against the real server, followed by their owning journey lanes.

No Wave 1 finding was rejected. TASK-REV-011-1 and STAT-1 are one issue. I revised STAT-2's proof to require a whole unscored report, not merely an inconclusive top-level verdict. The current recorded green lanes are credible for their stated scope; they do not close the seven findings above.
