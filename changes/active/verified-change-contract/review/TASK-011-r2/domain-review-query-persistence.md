# TASK-011 r2 domain review: query consistency and persistence

**Subject:** `/home/thorrester/Documents/GitHub/wyrd-vcc-t005`; base `338f33235f81c30dfe3a570dc26934fe7bb77048`; candidate `3c6fc1880692cc29c4c1ff7fc5fcb72a850bb9d6`. Reviewed the cumulative diff, approved specification revision 38, original TASK-011, r1 verdict and validation, and TASK-011-R1 remediation. **Result: PASS.**

## Boundary, authority, and source coverage

| Boundary | Authority | Source and determination |
|---|---|---|
| One consistent observation cut, fixed SQL, and audited reads | Spec REQ-153–156, AC-034, INV-012; TASK-011 and R1 FIND-TASK-011-3; `AGENTS.md` §§2, 9, 11; `architecture/agent-rules.md`; `architecture/bifrost-design.md` query and authorization rules | `wyrd-server/src/verification/drift.rs:113–359,367–509,803–901`; `query/scheduled.rs`; Oracle `planner.rs:383–450`, `oracle/mod.rs:3775–3835`. `psi_statement` and `spc_statement` combine the completeness count and every feature aggregate in one `UNION ALL` statement, globally ordered by part and subgroup. `Reader::fold` submits that statement once through the authenticated scheduled-query consumer. Oracle collects distinct table references and pins one sealed cut for the query; each repeated observation-table scan uses its registered provider. Subject, series, and half-open window filters remain on each component. PASS. |
| Incomplete target and report publication | Spec REQ-154, REQ-156, AC-012, AC-034; R1 FIND-TASK-011-1/2; `architecture/wyrd-design.md` Verifier result model | `drift.rs:161–201,383–509,671–754`; `vala-drift/src/spc/mod.rs` scorer; `wyrd-server/src/verification/results.rs:205–263,919–929`; `verification/runner.rs:375–412`. Completeness selects records with configured series present, including null values, and checks omitted and invalid values. `DistributionFold::finish` yields `None` for incomplete/invalid windows and an SPC scorer with an empty unscored report. `VerifierReport::Drift(None)` publishes an inconclusive summary with null details and no feature batch; failure dispatch does not follow from it. PASS. |
| Fitted version, tenant persistence, and historical reads | Spec REQ-157, AC-034; TASK-011 version constraint; `AGENTS.md` tenant/persistence rules; `architecture/references/domain/drift-monitoring.md` | `vala-drift/src/baseline/mod.rs:18–33`, `psi/mod.rs`, `spc/mod.rs`; `wyrd-server/src/verification/drift.rs:756–800`; `wyrd-sql/src/queries/drift_baselines.rs:361–374`; `wyrd-testing/src/verification.rs:322–348`; Rust/Python/TypeScript Drift journeys. The fitter writes format 2, and the server retrieves the ready fit through a tenant connection and rejects a missing/old marker before deserialization and scoring. Retiring a stored fit does not mutate prior result rows; all three journeys query an earlier scored result after the refusal. PASS. |
| Schedule and test controls | Spec AC-034 and INV-012; `AGENTS.md` test-tier and server-owned behavior rules | `wyrd-testing/src/verification.rs:299–348`, `python.rs:489–538`, `server.rs:3472–3486`; `wyrd-sdk-ts/native-testing/src/lib.rs:168–224`; Python/TypeScript/Rust Drift journeys. The test-only fixture makes a binding due in PostgreSQL and removes the format marker of a ready fit. The real runtime schedules; the SDKs start/read runs and inspect dispatches. These controls do not create a product route or duplicate scheduler and fit logic. PASS. |

## Findings

No material findings in this domain. The prior query-cut finding, **FIND-TASK-011-3**, is closed by the single statement and one query-service read. The prior whole-run unscored result gap, **FIND-TASK-011-2**, is closed through `SpcScorer::finish`, `DistributionFold::finish`, and the existing result builder. The persistence-side portion of **FIND-TASK-011-7** is closed by stored-fit refusal and historical-result reads in each SDK journey.

## Verification limits

This was source and evidence review; I did not rerun the reported green lanes. `one_statement_decides_completeness_and_scores_from_one_cut` executes the generated SQL over in-memory DataFusion data with and without an observation at the former read seam. It does not schedule a concurrent ingest against Oracle. The source path establishes the relevant invariant: one logical Oracle query pins the observation table once and uses that cut for all `UNION ALL` arms. The Python and TypeScript legacy tests remove a current fit's format marker through a test fixture; they do not load an archived revision-36 fit, but the server's pre-deserialization marker check covers that stored shape. JSON SDK observation APIs cannot carry NaN or infinity; Vala and server SQL tests cover those values.
