# TASK-011 r3 repository standards review

## Subject and authority coverage

Repository: `/home/thorrester/Documents/GitHub/wyrd-vcc-t005`; base `338f33235f81c30dfe3a570dc26934fe7bb77048`; candidate `c5c7a76cc1f45f5bdfad20de35a957b9f2f9ce57`. I inspected the cumulative diff, original TASK-011, R1/R2 remediation and review history, and the recorded final-candidate verification. The candidate is HEAD; `.codegraph/` is absent. This report does not assess task acceptance or statistical correctness.

| Changed surface | Governing authority inspected |
|---|---|
| Approved spec, original and remediation tasks, evidence and architecture docs | `AGENTS.md` §§1–2, 11, 14–16; `architecture/agent-rules.md`; `architecture/wyrd-design.md`; `architecture/wyrd-doctrine.mdx`; `architecture/references/README.md`, `languages/spec-driven-development.md`, `languages/implementation-execution.md`; approved spec revision 38 |
| Vala PSI/SPC, Arrow, fitted profiles and result types | `AGENTS.md` §§3–6, 10–11, 16; `architecture/agent-rules.md`; `architecture/references/architecture/patterns.md`, `domain/vala-architecture.md`, `domain/drift-monitoring.md`, `domain/arrow-analytical-interop.md`, `languages/rust-core.md`, `languages/testing-workflows.md` |
| `wyrd-spec` Drift Card validation and generated JSON schemas | `AGENTS.md` §§2–4, 9, 11, 16; `architecture/agent-rules.md`; `architecture/wyrd-design.md`; `architecture/wyrd-doctrine.mdx`; `architecture/references/architecture/patterns.md`, `languages/rust-core.md`, `languages/testing-workflows.md` |
| Server query/fold, test server and persistent fixture | `AGENTS.md` §§2–6, 9–11, 16; `architecture/agent-rules.md`; `architecture/wyrd-design.md`; `architecture/bifrost-design.md`; `architecture/references/domain/olap-serving.md`, `domain/drift-monitoring.md`, `languages/rust-core.md`, `languages/testing-workflows.md` |
| Rust, Python and TypeScript journeys and test-only language bindings | `AGENTS.md` §§7–8, 11, 16; `architecture/agent-rules.md`; `architecture/references/languages/python-api-and-stubs.md`, `languages/typescript-guide.md`, `languages/testing-workflows.md` |
| Drift design docs and generated `docs/public/llms-full.txt` | `AGENTS.md` §§1–2, 11, 14; `architecture/wyrd-design.md`; `architecture/wyrd-doctrine.mdx`; approved specification |

## Rule results

| Applicable rule | Result | Source and verification evidence |
|---|---|---|
| Required owner and dependency boundaries (`AGENTS.md` §§2–3, 5, 9; `architecture/patterns.md`) | PASS | Pure SPC profile validation remains in `wyrd-spec`; math and report in `vala-drift`; server owns fixed audited query and result mapping; SDK edits are journeys and private test projections. No dependency, Cargo feature, migration or public route entered the diff. |
| Tenant-safe Bifrost query and audit path (`AGENTS.md` §§2, 9; `bifrost-design.md`; `olap-serving.md`) | PASS for standards | Server retains the existing scoped SYSTEM token and query-service path. PSI/SPC fold one statement through Oracle. Domain reviewers own detailed query and tenancy correctness. |
| SQL transaction and test fixture ownership (`agent-rules.md`) | PASS | `VerificationFixture::retire_fitted_format` opens and commits its own `TenantConn`; no callee accepting `&mut TenantConn` commits it. Added Python/TypeScript controls delegate to this test owner. |
| Struct-centered, synchronous pure computation (`AGENTS.md` §§5–6; `agent-rules.md`) | PASS | `ObservationWindow`, `DistributionFold` and `SpcScorer` retain cohesive state and methods; pure PSI count and subgroup calculations remain synchronous. No new single-implementation trait or dependency appears. |
| Rustdoc and imported bare types (`AGENTS.md` §16; `agent-rules.md`) | PASS | R2 now adds `# Errors` to `ColumnRef::{collect_f64,collect_string}` and `# Panics` to the test helper; changed fallible test helper `spc_fit::fit` documents errors and panic. `fold_spc`, test fixture APIs, and TS native helper signatures use module-imported bare types. The cumulative changed items were checked for undocumented new items and added lint suppressions; none found. |
| Generated contracts and runtime OpenAPI (`AGENTS.md` §§8–9, 11; `agent-rules.md`) | PASS | SPC schema snapshots derive from `wyrd-spec` source; recorded `codegen:check` and served `test:principals:integration` pass. The private testing declaration is NAPI-generated by `ts:build:testing`, a dependency of recorded `ts:typecheck`. |
| Three first-class journey runtimes and correct test tier (`AGENTS.md` §11; language/testing references) | PASS | Rust, Python and TypeScript journey lanes passed after the last code change. Python tests remain top-level functions; TypeScript test-only controls do not enter the public SDK. |
| Focused named tests and owning lanes (`AGENTS.md` §11; `agent-rules.md`; `spec-driven-development.md`) | PASS | TASK-011 now records exact `mise exec -- cargo nextest run` commands/results for both named `wyrd-spec` tests, 25 Vala tests, 8 server tests, 2 Rust journeys, and focused Python and TypeScript selectors. Final-candidate results include `test:wyrd` 2142, `test:vala` 1280, server 85, journey lanes 2/40/20, format/lints, `py:lints`, `ts:typecheck`, OpenAPI, codegen, docs and `git diff --check`; `py:format` is also recorded in the original task evidence. |
| Test integrity (`AGENTS.md` §12; `agent-rules.md`) | PASS | R2 sparse PSI assertions change to the approved whole-run unscored result and still use `assert_unscored`, which checks inconclusive, no details and no feature rows. The server single-statement fixture grows from four to 100 records to retain a scored case. No added `#[allow]` or ignored test appears in the cumulative diff. |
| Task status progression (`AGENTS.md` §14; `spec-driven-development.md` “Task contract”) | FAIL | Original TASK-011 is `status: review`, but both implemented R1 and R2 remediation tasks remain `status: ready`; see S1. |

## Material finding

### S1 — Implemented remediation tasks still say `ready`

- **Violated rule:** `AGENTS.md` §14 and `architecture/references/languages/spec-driven-development.md` define `ready` as executable work and `review` as the status after implementation, before acceptance.
- **Location and evidence:** `changes/active/verified-change-contract/review/TASK-011-r1/TASK-011-R1-production-drift-closure.md:4` and `changes/active/verified-change-contract/review/TASK-011-r2/TASK-011-R2-production-drift-closure.md:4` both say `status: ready`; their implementation-evidence sections record completed work and passing verification, and original TASK-011 now says `status: review`.
- **Consequence:** The active packet presents both completed remediation tasks as still awaiting implementation, so a later workflow can pick them up again or misread the task state despite their evidence.
- **Testable correction:** Set the two remediation task front-matter statuses to `review` and leave their historical diagnoses and evidence unchanged. Verify the metadata by reading the task headers; no runtime test is needed.

## Overall result

**FAIL** — S1 is a bounded repository workflow-rule violation. The prior R2 standards findings on rustdoc, signature imports and `wyrd-spec` test execution are closed. This result does not decide statistical or task acceptance.
