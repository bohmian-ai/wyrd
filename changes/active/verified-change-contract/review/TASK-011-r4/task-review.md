# TASK-011 r4 task implementation review

**Result: PASS.** Reviewed the immutable cumulative range `338f33235f81c30dfe3a570dc26934fe7bb77048..1268bbe3ef820ba50e1c6dbb065d4c01b415f5f3` against approved specification revision 38, original TASK-011, prior r1–r3 verdicts and ledgers, and the R1–R3 remediation tasks. The repository has no `.codegraph/` directory. This is a static source and evidence review; I did not rerun the implementation lanes or change source. Candidate was HEAD during inspection.

| Requirement, criterion, constraint, or non-goal | Implementation evidence | Verification evidence | Result |
|---|---|---|---|
| REQ-153: exhaustive frozen PSI bins, unseen categorical values in `other`, 100-sample minimum for every feature, threshold behavior | `vala-drift/src/psi/mod.rs` fits exhaustive numeric/categorical bins and `score_psi_counts` checks every feature's sample before scoring; server `ObservationWindow::{psi_numeric,psi_categorical}` counts into fitted bins. | Named Vala bin, smoothing, equality, short-feature tests; SQL/fold tests; Rust/Python/TypeScript Drift journeys recorded in TASK-011. | PASS |
| REQ-154: authored fixed SPC subgroup size, at least 20 complete fit groups, ordered complete target groups | `wyrd-spec/src/card/drift.rs` has `sample_size` only; `spc/mod.rs` enforces complete fit and target groups; server `ObservationWindow::spc` orders by `created_at, record_id`; `SpcScorer::finish` makes any empty/partial feature unscored. | Contract validation, SPC fit/scorer, server SQL/fold, and all three SDK journey tests. | PASS |
| REQ-155: NIST X-bar/S formula, strict two-chart signals and typed evidence | `spc/control_limits.rs` uses sample SD, `c4`, `sqrt(n)` and strict bounds; `SpcScorer` counts chart signals and builds typed subgroup/chart evidence. | Independent formula and published constant fixtures; equality/signal tests; SDK evidence assertions. | PASS |
| REQ-156 and revision-38 selected-row contract: bad baseline fails; direct selected and server window targets with missing/null/non-finite/insufficient values are wholly unscored; unrelated rows excluded | `feature.rs::target_complete` checks every selected row; direct PSI `target_column` and direct SPC classify a present Arrow `Null` column as incomplete while preserving errors for other wrong types; `ObservationWindow::incomplete` checks configured series counts and values in the same query as aggregates; `DistributionFold::finish` maps an empty report to no details. | Numeric/categorical PSI and SPC `NullArray` tests, typed-null and wrong-type tests, server repeated-series and one-statement tests, sparse journey cases. | PASS |
| REQ-157: new fit format, old-fit refusal, historical reads | Fitted state carries `FITTED_FORMAT`; server `DriftEngine::fitted` refuses legacy format; no old scorer or migration path. | Rust, Python and TypeScript journeys retire a stored fit and read the earlier result. | PASS |
| INV-012 and TASK-011 preservation: tenant, subject, series, half-open window, audited query service, bounded runtime, Custom, result and Operator paths | `ObservationWindow::rows_in` preserves subject and `[start,end)` selection; `Reader` uses scoped SYSTEM token and scheduled query service; PSI/SPC use one Oracle statement; Custom branch and shared runtime/result publication remain. | Recorded Bifrost server and three SDK journey lanes, OpenAPI, and existing TASK-005 controls. | PASS |
| AC-034: statistical fixtures, direct/server agreement, scheduled failure dispatch, visible old-fit refusal in all first-class SDKs | Vala direct and server aggregate paths converge on shared PSI and SPC scoring; test-only schedule/retired-fit controls drive real SDK-to-server journeys. | Exact named commands and passed lanes recorded in TASK-011 and remediation evidence. JSON SDK observations cannot carry NaN/inf; Vala and server SQL tests cover those values. | PASS |
| Non-goals and scope: no extra chart, configurable rules, imputation, PSI missing bin, new observation format, client scorer, migration, or second read cut | Complete cumulative diff removes WECO and does not add excluded mechanisms. | Diff inspection and `git diff --check`. | PASS |
| Prior FIND-TASK-011-1 through -7 | Selected direct rows, whole-run SPC unscoring, single query cut, served OpenAPI proof, exact commands, original task status, Python/TS scheduled and old-fit journeys are present. | R1 source and recorded evidence. | PASS |
| Prior FIND-TASK-011-8 through -11 | All-feature PSI floor, duplicate series refusal, Rust docs/import corrections, exact `wyrd-spec` tests are present. | R2 source and recorded evidence. | PASS |
| Prior FIND-TASK-011-12 and -13 | Direct PSI/SPC `DataType::Null` target handling and all R1–R3 remediation `status: review` headers are present at candidate. | Focused null-type tests and front-matter inspection. | PASS |

**Proposed findings:** none. One Oracle statement supplies the common query cut; no mid-query ingest race test is recorded. The 99-record duplicate case is proven at SQL/fold level; existing journeys prove the ordinary inconclusive publication path. These limits do not leave an unmet TASK-011 obligation.
