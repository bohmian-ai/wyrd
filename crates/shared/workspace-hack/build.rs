//! Build script that makes Cargo apply this crate's unified build-dependency
//! features to procedural macros and build scripts across the workspace.

/// Does nothing; the script's presence is what matters.
fn main() {}
