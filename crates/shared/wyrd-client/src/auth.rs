//! Authentication middleware.
//!
//! `AuthMiddleware` (Arc-shared token exchange, proactive/reactive refresh,
//! and the `wyrd-request-id` spine) lands in commit 05. This module is a
//! placeholder so the crate module tree resolves.
