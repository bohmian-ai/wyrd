# skald-providers Fixtures

These fixtures are local, credential-free provider examples for HTTP mock tests.
They are hand-authored from public provider API shapes and redacted placeholders;
no live provider calls or secrets are required to run the suite.
