//! Test-only whole-request projection oracle for parity verification.

pub use crate::gate::collector::{
    ProjectedExport, project_resource_logs, project_resource_metrics, project_resource_spans,
};
