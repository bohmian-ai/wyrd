{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "WebhookConfig",
  "description": "Configuration for a signed HTTP webhook destination.\n\nUsed by `crate::alert_router::signer` (PR4.3) to deliver alert notifications to external receivers. The signing protocol is HMAC-SHA256 over a canonical string; see the module-level doc for the exact format.\n\nWhen `secret_ref` is `None`, the webhook is delivered unsigned. Unsigned delivery is only recommended for internal endpoints that enforce network-level isolation.\n\n# Default\n\n`WebhookConfig::default()` returns a config with the canonical Wyrd header names, `WEBHOOK_DEFAULT_TIMEOUT_MS`, no `secret_ref`, no `tls`, and an empty `headers` map. The `url` field defaults to an empty string: the `Default` impl is a starting point for builders and tests, not a deployable config. `WebhookConfig::default().validate()` returns `Err` because of the empty URL, by design.",
  "type": "object",
  "properties": {
    "headers": {
      "description": "Non-sensitive static HTTP headers to include on every request. The map is ordered (`BTreeMap`) for deterministic serialization.\n\n**Plaintext secrets MUST NOT be stored here.** `validate()` rejects the config if any entry collides with `Authorization`, `Cookie`, the configured `signature_header`, the configured `timestamp_header`, or carries a secret-shaped name (case-insensitive match against `authorization`, `cookie`, names ending in `-auth`, `-token`, `-key`, `-secret`, `-credential`, plus `x-api-key`, `x-amz-security-token`). Header-name comparison is case-insensitive. Use [`secret_headers`](Self::secret_headers) for credential values so resolution happens at send time via the Wyrd auth shell and is never serialized to disk.\n\nExamples of values that belong here: `Content-Type` overrides, `User-Agent`, `X-App-Name`, `X-Request-Id` correlation headers.",
      "default": {},
      "type": "object",
      "additionalProperties": {
        "type": "string"
      }
    },
    "method": {
      "description": "HTTP method for delivery. Typically `Post`; some receivers require `Put`.\n\nDefault: `WebhookMethod::Post`.",
      "default": "POST",
      "allOf": [
        {
          "$ref": "#/definitions/WebhookMethod"
        }
      ]
    },
    "secret_headers": {
      "description": "Sensitive HTTP headers whose values resolve from the runtime secret store. The signer (PR4.3) resolves each `SecretRef` via `wyrd_auth::resolve` immediately before sending the request and never persists the plaintext. The key is the header name. The same case-insensitive signing/timestamp collision rules as [`headers`](Self::headers) apply, but `Authorization` is permitted here because that is exactly the case `SecretRef` indirection is designed for. The value is the secret reference.\n\n`validate()` rejects duplicate keys across `headers` and `secret_headers` so callers cannot accidentally shadow a credential with a plaintext value.\n\nDefault: empty. The field is omitted from serialized output when empty (`skip_serializing_if`).",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/definitions/SecretRef"
      }
    },
    "secret_ref": {
      "description": "HMAC-SHA256 signing key. The signature is carried on the header named by [`signature_header`](Self::signature_header). `None` disables signing, which is recommended only for internal endpoints.",
      "anyOf": [
        {
          "$ref": "#/definitions/SecretRef"
        },
        {
          "type": "null"
        }
      ]
    },
    "signature_header": {
      "description": "Name of the HTTP header that carries the hex-encoded HMAC-SHA256 signature.\n\nDefault: [`WEBHOOK_SIGNATURE_HEADER_DEFAULT`] (`\"X-Wyrd-Signature\"`).",
      "default": "X-Wyrd-Signature",
      "type": "string"
    },
    "timeout_ms": {
      "description": "Request timeout in milliseconds.\n\nDefault: [`WEBHOOK_DEFAULT_TIMEOUT_MS`] (`10_000`, or 10 s).",
      "default": 10000,
      "type": "integer",
      "format": "uint64",
      "minimum": 0.0
    },
    "timestamp_header": {
      "description": "Name of the HTTP header that carries the Unix timestamp in seconds used in the canonical signing string.\n\nDefault: [`WEBHOOK_TIMESTAMP_HEADER_DEFAULT`] (`\"X-Wyrd-Timestamp\"`).",
      "default": "X-Wyrd-Timestamp",
      "type": "string"
    },
    "tls": {
      "description": "Optional TLS configuration. When `None`, the scheme in `url` governs TLS.",
      "anyOf": [
        {
          "$ref": "#/definitions/TlsConfig"
        },
        {
          "type": "null"
        }
      ]
    },
    "url": {
      "description": "Destination URL. Must be non-empty.\n\nExample: `\"https://hooks.example.com/wyrd-alerts\"`.\n\nThe `Default` impl leaves this empty so callers explicitly set it. `validate()` rejects an empty URL.",
      "default": "",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "definitions": {
    "SecretRef": {
      "description": "Pointer into a Wyrd runtime secret source. Production builds support environment variables, mounted files, and external secret-manager keys.",
      "oneOf": [
        {
          "description": "An environment variable name. The runtime reads `std::env::var(name)` at connect time. Conventionally `SCREAMING_SNAKE_CASE`.\n\nExample: `{\"source\": \"env\", \"name\": \"WYRD_SECRET_API_KEY\"}`.",
          "type": "object",
          "required": [
            "name",
            "source"
          ],
          "properties": {
            "name": {
              "description": "Environment variable name. Must be non-empty.",
              "type": "string"
            },
            "source": {
              "type": "string",
              "enum": [
                "env"
              ]
            }
          },
          "additionalProperties": false
        },
        {
          "description": "An absolute path to a file on the local filesystem. Kubernetes Secret volume mounts land here.\n\nExample: `{\"source\": \"file\", \"path\": \"/var/run/secrets/wyrd/api-key\"}`.",
          "type": "object",
          "required": [
            "path",
            "source"
          ],
          "properties": {
            "path": {
              "description": "Absolute path to a file whose contents are the secret value. Must be non-empty.",
              "type": "string"
            },
            "source": {
              "type": "string",
              "enum": [
                "file"
              ]
            }
          },
          "additionalProperties": false
        },
        {
          "description": "A backend-agnostic key in an external secret manager (HashiCorp Vault, AWS Secrets Manager, Google Secret Manager). The Wyrd auth shell maps the key to the configured backend.\n\nExample: `{\"source\": \"vault\", \"key\": \"secret/data/wyrd/api-key\"}`.",
          "type": "object",
          "required": [
            "key",
            "source"
          ],
          "properties": {
            "key": {
              "description": "Backend-agnostic key path. Interpretation is backend-specific.",
              "type": "string"
            },
            "source": {
              "type": "string",
              "enum": [
                "vault"
              ]
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "TlsConfig": {
      "description": "TLS configuration for a transport connection.\n\nAll certificate fields are optional; an absent field means \"use the system default / skip that verification step\". Setting `insecure_skip_verify` to `true` disables peer certificate validation and SHOULD NOT be used outside local development.",
      "type": "object",
      "properties": {
        "ca_cert": {
          "description": "PEM-encoded CA certificate bundle. When set, overrides the system CA pool for this connection.",
          "anyOf": [
            {
              "$ref": "#/definitions/SecretRef"
            },
            {
              "type": "null"
            }
          ]
        },
        "client_cert": {
          "description": "PEM-encoded client certificate for mTLS. Must be paired with `client_key`.",
          "anyOf": [
            {
              "$ref": "#/definitions/SecretRef"
            },
            {
              "type": "null"
            }
          ]
        },
        "client_key": {
          "description": "PEM-encoded client private key for mTLS. Must be paired with `client_cert`.",
          "anyOf": [
            {
              "$ref": "#/definitions/SecretRef"
            },
            {
              "type": "null"
            }
          ]
        },
        "insecure_skip_verify": {
          "description": "Disable peer certificate verification. Defaults to `false`.\n\n**WARNING:** setting this to `true` opens the connection to man-in-the-middle attacks. Use only in local development.",
          "default": false,
          "type": "boolean"
        },
        "server_name_override": {
          "description": "Override the server name used in SNI and certificate verification. Useful when the server's certificate CN does not match the address in `endpoint`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "WebhookMethod": {
      "description": "HTTP method for webhook delivery.\n\nWire representation is uppercase HTTP verb: `\"POST\"` or `\"PUT\"`.",
      "oneOf": [
        {
          "description": "HTTP POST.",
          "type": "string",
          "enum": [
            "POST"
          ]
        },
        {
          "description": "HTTP PUT.",
          "type": "string",
          "enum": [
            "PUT"
          ]
        }
      ]
    }
  }
}
