# Trigger Card - activates a bound Drift Verifier on a schedule.

apiVersion: wyrd/v1
kind: Trigger
metadata:
  space: retention
  name: churn-drift-trigger
  version: 1.0.0

spec:
  kind: schedule
  cron: "*/5 * * * *"
  tz: UTC
