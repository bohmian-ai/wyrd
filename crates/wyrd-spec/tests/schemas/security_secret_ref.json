{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "SecretRef",
  "description": "Pointer into the Wyrd runtime secret store.\n\nProduction sources point at environment variables, mounted files, or an external secret manager. Test-only helpers are excluded from production schemas.",
  "oneOf": [
    {
      "description": "Environment variable name.",
      "type": "object",
      "required": [
        "name",
        "source"
      ],
      "properties": {
        "name": {
          "description": "Environment variable name. Must be non-empty when resolved.",
          "type": "string"
        },
        "source": {
          "type": "string",
          "enum": [
            "env"
          ]
        }
      },
      "additionalProperties": false
    },
    {
      "description": "Local filesystem path to a file containing the secret value.",
      "type": "object",
      "required": [
        "path",
        "source"
      ],
      "properties": {
        "path": {
          "description": "File path. Must be non-empty when resolved.",
          "type": "string"
        },
        "source": {
          "type": "string",
          "enum": [
            "file"
          ]
        }
      },
      "additionalProperties": false
    },
    {
      "description": "Backend-agnostic key in an external secret manager.",
      "type": "object",
      "required": [
        "key",
        "source"
      ],
      "properties": {
        "key": {
          "description": "Secret-manager key path. Interpretation is backend-specific.",
          "type": "string"
        },
        "source": {
          "type": "string",
          "enum": [
            "vault"
          ]
        }
      },
      "additionalProperties": false
    }
  ]
}
