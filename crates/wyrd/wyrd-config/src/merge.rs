//! TODO(commit 05): apply_defaults pure fn.

use wyrd_spec::envelope::{CardKind, Metadata};

use crate::config::WyrdConfig;

/// Apply workspace defaults to card metadata (stub).
pub fn apply_defaults(_meta: &mut Metadata, _kind: CardKind, _cfg: &WyrdConfig) {}
