[package]
name = "wyrd-observe-impl"
version = { workspace = true }
edition = { workspace = true }
license = { workspace = true }
rust-version = { workspace = true }
repository = { workspace = true }
authors = { workspace = true }
publish = false
description = "Bridge from Wyrd observer state into the Skald agent observer provider slot."

[lib]
path = "src/lib.rs"

[features]
default = []
python = ["dep:pyo3", "dep:tokio", "dep:skald-prompt", "skald-prompt/python"]

[dependencies]
skald-agent = { workspace = true }
skald-prompt = { workspace = true, optional = true }
skald-spec = { workspace = true }
wyrd-observe = { workspace = true }
async-trait = { workspace = true }
pyo3 = { workspace = true, optional = true }
tokio = { workspace = true, optional = true, features = ["rt"] }

[dev-dependencies]
async-trait = { workspace = true }
tokio = { workspace = true, features = ["macros", "rt-multi-thread"] }
