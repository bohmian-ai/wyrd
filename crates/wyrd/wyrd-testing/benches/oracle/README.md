# Oracle calibration artifacts

The calibration lane writes a complete JSON report, Markdown summary, and
candidate TOML profile here. The checked-in profile is intentionally marked
`candidate`; production activation requires maintainer approval.
