---
interface Props {
  phase: string;
  label?: string;
}

const { phase, label = `Phase ${phase}` } = Astro.props;
---

<aside class="wyrd-phase-gate" data-phase={phase}>
  <span class="wyrd-phase-gate__badge">{label}</span>
  <slot />
</aside>
