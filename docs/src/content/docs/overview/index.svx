---
title: Overview
description: What Wyrd is, the coordination problem it solves, and what runs today.
pillar: wyrd
group: Overview
order: 1
---

# Overview

Wyrd is the AI layer for human and agentic work. It makes AI work declarative,
connectable, inspectable, reproducible, and operable.

Concretely: Wyrd connects every piece of an AI system. Declare cards for every
component, connect them with typed references, record observations at runtime, and
evaluate, audit, and alert from one contract.

## The coordination problem

An AI system is not one artifact. It is a dataset, the model trained on it, the
prompt that calls the model, the agent that runs the prompt, and the workflow
that orchestrates the agents. It is also the pre-deployment observations
(commits, code changes, agent traces), the post-deployment observations
(traces, evals, logs, metrics, drift), and the glue connecting all of it.
Most organizations never build that glue. It is fragile, hard to maintain,
and requires wiring together systems that were never designed to coordinate.

Each piece usually lives in a different tool with a different format: a
feature store, a model registry, a prompt file in a repo, a YAML config, a
notebook.

Nothing types the boundaries between them, so nothing can answer basic questions:

- Which dataset trained the model this prompt calls?
- If I change this prompt, what agents depend on it?
- Is the prompt running in production the one that was reviewed?

Those are the structural questions. There is a second problem running in
parallel. Most teams are already collecting data that would answer harder
ones: commits, agent traces, evals, logs, metrics, drift signals. That data
exists. It sits in systems that have no concept of the components that
generated it, so it cannot be connected back to anything. When a model
starts drifting, you cannot tell from the metrics alone which data trained
it, which prompts call it, or which agent workflows will be affected. AI
system behavior is a function of how components communicate and fail, not
just what they are.

Every team rebuilds this connective tissue by hand with naming conventions,
wiki pages, and tribal knowledge, and it rots. Wyrd's premise is that the
connective tissue should be typed and derived, not documented, and that
observations should connect to that structure rather than sit in a separate
silo you only query when something breaks.

## How Wyrd answers it

You describe the things that matter as cards. Wyrd records the descriptions,
validates them, links them, and tracks what happens. The loop is four verbs:

1. **Author** a card in Python: a `DataCard`, `ModelCard`, or `PromptCard`.
2. **Register** it: the server validates the spec and stores it under an
   immutable `(space, name, version)` identity. Registration is the HTTP API
   today; the Python package authors cards locally.
3. **Refer** to other cards with a `CardRef`. The server derives the lineage
   graph from those refs. You never author edges directly.
4. **Run**: Skald resolves cards from the registry at execution time. The prompt
   you registered is the prompt that runs.

Every card, of every kind, shares one envelope: `apiVersion`, `metadata`, `kind`,
`spec`, `relationships`, `status`. The `kind` selects the spec schema; everything
else is constant. That single shape makes the whole system inspectable from one contract. See [Card](/concepts/card/) for the envelope in detail.

## What runs today

<Aside title="Three kinds run end-to-end" data-variant="wyrd">

**Data**, **Model**, and **Prompt** are fully implemented: authored in Python,
validated, registered, referenced, and resolvable at runtime. **Skald** runs
agents and workflows over registered prompts today.

The other thirteen kinds plus `External` are reserved structure: their specs and
schemas are authored, but there is no runtime behind them yet. Every page that
references a reserved kind says so. See [Cards](/cards/) for per-kind status.

</Aside>

## Where to go next

- [Get started](/get-started/): install Wyrd and register your first card.
- [Where Wyrd fits](/overview/where-wyrd-fits/): the AI-stack position, the
  Mitari platform, and what Wyrd is deliberately not.
- [Concepts](/concepts/): the Card / Spec / Run / Observation ontology.
- [For agents](/for-agents/): stable paths, a copyable workflow, and the rules
  agents follow.
