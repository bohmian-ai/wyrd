---
title: Authentication
description: The principals, tokens, and grants the server uses, plus generating and mounting the signing and sealing keys.
pillar: wyrd
group: Self-hosting
order: 7
---

# Authentication

Wyrd authenticates every request with a signed token. There is no session cookie and no ambient identity: a caller presents a bearer token in the `X-Wyrd-Access-Token: Bearer <token>` header, and that token is the only credential the server trusts per request. This page is the operator view. For the deeper model behind it, read [Authentication](/concepts/authentication/) and [Authorization](/concepts/authorization/) in Concepts.

## Principals

Every token belongs to a principal, the identity that obtained it. Three kinds exist.

<DataTable
  columns={[
    { header: 'Kind', role: 'name' },
    { header: 'How it authenticates' },
    { header: 'Bound to' }
  ]}
  rows={[
    ['User', 'Human OIDC login through an external IdP', 'An email, no card'],
    ['Service', 'API key, or workload JWT (jwt-bearer)', 'A ServiceCard'],
    ['Agent', 'API key, or workload JWT (jwt-bearer)', 'An AgentCard']
  ]}
/>

Services and Agents authenticate the same way. The difference is the kind of card they are bound to.

## Tokens

Wyrd mints EdDSA (Ed25519) JWTs signed with its own key. Access tokens are short-lived and required on every authenticated request. Refresh tokens are issued alongside for the grants that support rotation (human login and API-key exchange); they are single-use and rotate on each exchange. Workload grants issue an access token only, because a workload can always re-assert its identity through its platform.

Verification runs five checks on every request: the EdDSA signature against the named key, the `iss` claim, expiry within the configured skew, the tenant binding, and the principal's revocation epoch. Any failure is a `401`.

## Grants

| Grant | Route | Issues refresh? |
| --- | --- | --- |
| `wyrd_api_key` | `POST /auth/token` | Yes |
| `jwt-bearer` (workload) | `POST /auth/token` | No |
| `authorization_code` (OIDC) | `POST /auth/token` | Yes |
| `token-exchange` (delegation) | `POST /auth/token` | No |
| `refresh_token` | `POST /auth/token` | Yes (rotated) |

Human login and machine workload federation are set up in [SSO and OIDC](/self-hosting/sso-and-oidc/). The rest of this page is the key material an operator provides.

## Signing key

The signing key is Wyrd's own Ed25519 private key. It signs and verifies every Wyrd-issued token, so it is the one secret a production server cannot boot without. Provide it as a PKCS#8 PEM.

Generate one:

```bash
openssl genpkey -algorithm ed25519 -out signing-key.pem
```

Mount it by path (preferred) or inline:

```bash
export WYRD_SIGNING_KEY_FILE=/run/secrets/signing-key.pem
# or, less preferred:
export WYRD_SIGNING_KEY_PEM="$(cat signing-key.pem)"
```

In a production profile, boot fails closed if neither is set. In development the server mints an ephemeral key on each start and logs a warning, so local runs work with no secret. That key does not survive a restart, so tokens minted against it stop verifying when the process restarts. Never use an ephemeral key outside development.

<Aside title="Rotation">

Rotating the signing key invalidates every token signed with the old key. Plan a rotation for a window where callers can re-authenticate, or run the new and old keys in parallel if your deployment supports multiple verifying keys.

</Aside>

## Sealing key

The sealing key encrypts IdP client secrets at rest with AES-256-GCM. You only need it if a trusted issuer you configure carries a client secret (`secret_basic` or `secret_post` client auth). A deployment that uses only public PKCE clients and workload federation does not need one.

Generate a 32-byte key, base64-encoded, and put it in a file:

```bash
openssl rand -base64 32 > sealing-key.b64
export WYRD_SEALING_KEY_FILE=/run/secrets/sealing-key.b64
```

The file holds base64 that must decode to exactly 32 bytes. Boot fails closed if it decodes to any other length, or if an issuer needs a sealing key and none is set.

## First admin key

A fresh deployment has no credentials. Mint the first admin API key with the `bootstrap-key` subcommand, then use it to call the rest of the API. See [Running the server](/self-hosting/running-the-server/#bootstrap-key).

## Related

- [SSO and OIDC](/self-hosting/sso-and-oidc/): wiring an identity provider for human and workload login.
- [Authentication](/concepts/authentication/) and [Authorization](/concepts/authorization/): the model behind tokens and roles.
