# Examples

Examples are grouped by language so humans and agents can find runnable entry
points without searching crate internals.

- `examples/python/` contains Python examples.
- `examples/rust/` contains Rust examples.
