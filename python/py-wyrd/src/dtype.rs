//! Native placeholder for future `DataCard` dtype helpers.

/// Native-side dtype helper surface is intentionally thin until `wyrd-spec`
/// exposes the locked `DataCard` contract.
#[must_use]
pub fn dtype_helpers_ready() -> bool {
    true
}
